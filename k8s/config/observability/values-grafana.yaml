replicas: 1

# kustomize renders Helm test hooks as plain Pods, so ArgoCD would deploy the
# test pod as a permanent workload.
testFramework:
  enabled: false

deploymentStrategy:
  type: Recreate

envFromSecret: observability-secrets

grafana.ini:
  security:
    admin_password: $__env{GRAFANA_ADMIN_PASSWORD}
  server:
    root_url: "https://grafana.3istor.com"
  auth.generic_oauth:
    enabled: true
    name: "3Istor ID"
    allow_sign_up: true
    client_id: "3-istor-openid"
    client_secret: $__env{OIDC_CLIENT_SECRET}
    scopes: "openid profile email roles groups"
    auth_url: "https://auth.3istor.com/realms/3istor/protocol/openid-connect/auth"
    token_url: "https://auth.3istor.com/realms/3istor/protocol/openid-connect/token"
    api_url: "https://auth.3istor.com/realms/3istor/protocol/openid-connect/userinfo"
    role_attribute_path: "contains(groups[*], 'infra') && 'Admin' || contains(groups[*], 'member') && 'Viewer'"

persistence:
  enabled: true
  storageClassName: "ceph-block"
  size: 2Gi

resources:
  requests:
    cpu: 50m
    memory: 256Mi
  limits:
    cpu: 200m
    memory: 256Mi

# We don't provision datasources automatically because you will create them per Grafana Organization (Client A, Client B) manually to enforce multi-tenancy.
datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        uid: prometheus
        url: "http://vm-victoria-metrics-cluster-vmselect.observability.svc.cluster.local:8481/select/0/prometheus"
        access: proxy
        isDefault: true
        jsonData:
          prometheusType: "Prometheus"
          manageAlerts: false

      - name: Alertmanager
        type: alertmanager
        uid: alertmanager
        url: "http://alertmanager.observability.svc.cluster.local:9093"
        access: proxy
        jsonData:
          implementation: prometheus

      # Loki runs with auth_enabled, so every query needs an explicit tenant.
      - name: Loki
        type: loki
        uid: loki
        url: "http://loki.observability.svc.cluster.local:3100"
        access: proxy
        jsonData:
          httpHeaderName1: "X-Scope-OrgID"
        secureJsonData:
          httpHeaderValue1: "default"

# Configure dashboard providers.
# ref: http://docs.grafana.org/administration/provisioning/#dashboards
# `path` must be /var/lib/grafana/dashboards/<provider_name>
dashboardProviders:
  dashboardproviders.yaml:
    apiVersion: 1
    providers:
      - name: default
        orgId: 1
        folder: ""
        type: file
        disableDeletion: false
        editable: true
        allowUiUpdates: true
        options:
          path: /var/lib/grafana/dashboards/default

      - name: "kubernetes"
        orgId: 1
        folder: "Kubernetes"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/kubernetes

      - name: "infrastructure"
        orgId: 1
        folder: "Infrastructure"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/infrastructure

      - name: "gateway"
        orgId: 1
        folder: "Gateway API"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/gateway

      - name: "platform"
        orgId: 1
        folder: "Platform Services"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/platform

      - name: "observability"
        orgId: 1
        folder: "Observability"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/observability

      - name: "logs"
        orgId: 1
        folder: "Logs"
        type: file
        disableDeletion: false
        editable: true
        options:
          path: /var/lib/grafana/dashboards/logs

# Grafana Links: https://grafana.com/grafana/dashboards/<gnetId>
dashboards:
  # Cluster-wide views built on kube-state-metrics + cAdvisor + node-exporter.
  kubernetes:
    k8s-global:
      gnetId: 15757
      revision: 43
      datasource: Prometheus
    k8s-namespaces:
      gnetId: 15758
      revision: 46
      datasource: Prometheus
    k8s-nodes:
      gnetId: 15759
      revision: 40
      datasource: Prometheus
    k8s-pods:
      gnetId: 15760
      revision: 39
      datasource: Prometheus
    k8s-apiserver:
      gnetId: 15761
      revision: 21
      datasource: Prometheus

  # Bare-metal / OS level, from node-exporter.
  infrastructure:
    node-exporter-full:
      gnetId: 1860
      revision: 45
      datasource: Prometheus

  # Doc: https://gateway.envoyproxy.io/docs/tasks/observability/gateway-api-metrics/#dashboards
  gateway:
    gateway-api-gateways:
      gnetId: 19433
      revision: 2
      datasource: Prometheus
    gateway-api-gatewayclasses:
      gnetId: 19432
      revision: 2
      datasource: Prometheus
    gateway-api-httproutes:
      gnetId: 19434
      revision: 2
      datasource: Prometheus

  # Platform components. Each one needs its ServiceMonitor/PodMonitor enabled
  # in the matching k8s/config/<component>/values.yaml.
  platform:
    argocd:
      gnetId: 14584
      revision: 1
      datasource: Prometheus
    cert-manager:
      gnetId: 20842
      revision: 3
      datasource: Prometheus
    cloudnative-pg:
      gnetId: 20417
      revision: 4
      datasource: Prometheus

  # The monitoring stack watching itself.
  observability:
    victoriametrics-cluster:
      gnetId: 11176
      revision: 60
      datasource: Prometheus
    alertmanager:
      gnetId: 9578
      revision: 4
      datasource: Prometheus
    loki-global:
      gnetId: 13407
      revision: 1
      datasource: Prometheus

  # Log exploration, backed by the Loki datasource.
  logs:
    loki-kubernetes-logs:
      gnetId: 15141
      revision: 1
      datasource: Loki

# The Kyverno require-resources-and-memory-limits policy needs every container,
# including init containers, to declare requests with memory limit == request.
initChownData:
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      cpu: 100m
      memory: 64Mi

downloadDashboards:
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 64Mi
