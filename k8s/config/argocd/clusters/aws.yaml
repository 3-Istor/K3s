# Argo CD cluster registration for AWS. NOT referenced by kustomization.yaml —
# add it there once kvv2/argocd/clusters/aws exists in Vault, otherwise the
# operator reconciles a path that is not there and reports an error every cycle.
#
# The cluster team provides the API server endpoint, the CA and a service
# account token; those go into Vault, never into this file.
apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: cluster-aws
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: cluster
    cnp.3istor.com/cloud: aws
spec:
  path: kvv2/argocd/clusters/aws
  vaultRole: argocd-role
  type: Opaque
  keys:
    - name
    - server
    - config
