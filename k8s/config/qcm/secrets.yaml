apiVersion: ricoberger.de/v1alpha1
kind: VaultSecret
metadata:
  name: qcm-secrets
  namespace: qcm
spec:
  path: kvv2/qcm/config
  vaultRole: qcm-role
  type: Opaque
  keys:
    - database-url
    - database-password
    - nextauth-secret
    - client-secret
