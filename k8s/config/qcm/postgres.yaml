apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qcm-postgres-pvc
  namespace: qcm
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: qcm-postgres
  namespace: qcm
spec:
  selector:
    app: qcm-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qcm-postgres
  namespace: qcm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qcm-postgres
  template:
    metadata:
      labels:
        app: qcm-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: "qcm"
            - name: POSTGRES_USER
              value: "qcm"
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: qcm-secrets
                  key: DATABASE_PASSWORD
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres-data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: qcm-postgres-pvc
