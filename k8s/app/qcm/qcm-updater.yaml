apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: qcm-updater
  namespace: argocd
spec:
  applicationRefs:
    - namePattern: "qcm"
      images:
        - alias: "qcm"
          imageName: "ghcr.io/3-istor/qcm"

          manifestTargets:
            helm:
              name: "image.repository"
              tag: "image.tag"

          commonUpdateSettings:
            updateStrategy: "newest-build"
            allowTags: "regexp:^sha-[a-f0-9]+$"
      writeBackConfig:
        method: "git:secret:argocd/git-creds"
        gitConfig:
          branch: "main"
          writeBackTarget: "helmvalues:/k8s/config/qcm/qcm-values.yaml"
          pullRequest:
            github: {}
